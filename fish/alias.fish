# apps
alias vim nvim
alias ls exa
alias cat bat
alias rm trash

# ls
alias la "ls -a"
alias ll "ls --long"
alias lla "ls --long -a"
alias lt "ls --tree"

# misc
alias cl clear
alias reload 'exec fish'
alias mkdir "mkdir -p"
