# apps
alias vim nvim
alias ls lsd
alias cat bat
alias rm trash
alias git hub

# ls
alias l ll
alias lla 'll -a'
alias lt 'ls --tree'

# clear
alias cl clear

# edit dots
alias dothypr "vim ~/.config/hypr/hyprland.conf"
alias dotfish "vim ~/.config/fish/"
alias dotalias "vim ~/.config/fish/aliases.fish"
alias dotbar "vim ~/.config/waybar/"
alias dotvim "vim ~/.config/nvim/"
alias dotrofi "vim ~/.config/rofi/config.rasi"

# misc
alias reload 'exec fish'
alias mkdir "mkdir -p"

